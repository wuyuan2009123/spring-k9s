server:
  port: ${PORT:8761}
spring:
  application:
    name: eureka-service
management:
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    com:
      netflix:
        eureka:
          registry: error